package me.ray.aethelgardRPG.core.guis;

/**
 * Uma interface marcadora para GUIs que devem salvar e limpar o inventário do jogador
 * ao serem abertas, e restaurá-lo ao serem fechadas.
 * <p>
 * Qualquer classe de GUI que implemente esta interface será automaticamente gerenciada
 * pelo {@link ProtectedGUIManager}.
 */
public interface ProtectedGUI {
    // Nenhuma implementação é necessária. Esta é apenas uma interface marcadora.
}